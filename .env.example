# Mapy.com (Mapy.cz) API key — register at https://developer.mapy.com/
# Used for both geocoding (address → coordinates) and tile loading.
# Restrict the key by HTTP referrer in the dashboard for production.
NEXT_PUBLIC_MAPY_API_KEY=your_mapy_api_key_here
